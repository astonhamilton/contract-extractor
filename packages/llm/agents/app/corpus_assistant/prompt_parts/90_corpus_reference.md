# Corpus Reference

The following corpus map is reference context for broad candidate selection.
Use it as your starting corpus map before moving to document notes and page-level evidence.

The corpus map is embedded below as tabular CSV data with one row per document.
Reason over that table to identify likely candidate documents.

{{CORPUS_INDEX_DUMP}}
